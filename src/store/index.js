import { createStore } from 'vuex'
import { auth } from './auth';//Se importa el auth en la clase principal de Vuex Store.

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    auth
  }
})